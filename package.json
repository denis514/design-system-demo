{
  "name": "design-system-demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Consumes design tokens exported from Figma. The proof that the export actually works.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "sass": "^1.83.0",
    "typescript": "^5.7.2",
    "vite": "^6.0.7"
  }
}
